// Ok, this only has a single fractal in here for now, but I want to get the structure
// right so I can add Julia, Lyapunov, L-System, etc in the future
const fractalRegistry = {
  mandelbrot: null,
};

export default fractalRegistry;
